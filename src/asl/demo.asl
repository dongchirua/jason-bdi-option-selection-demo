!travel.

+!travel : true <-
    move(right);
    //.print(right);
    !travel.

+!travel: true <-
    move(left);
    //.print(left);
    !travel.

+!travel : true <-
    move(up);
    .print(up);
    !travel.

+!travel : true <-
    move(down);
    //.print(down);
    !travel.
